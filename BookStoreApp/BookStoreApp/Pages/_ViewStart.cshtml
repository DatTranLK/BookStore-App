@using Microsoft.AspNetCore.Http
@{
    if (Context.Session.GetString("Role") == "1" || Context.Session.GetString("Role") == "2")
    {
        Layout = null;
    }
    else
    {
        Layout = "_Layout";
    }
}
